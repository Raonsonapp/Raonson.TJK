import 'package:flutter/material.dart';

class ChatGPTScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ChatGPT Screen", style: TextStyle(color: Colors.white, fontSize: 24)),
      ),
    );
  }
}
