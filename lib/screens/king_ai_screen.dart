import 'package:flutter/material.dart';

class KingAIScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("KING AI Screen", style: TextStyle(color: Colors.white, fontSize: 24)),
      ),
    );
  }
}
